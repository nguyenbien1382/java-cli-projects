A collection of small command-line interface (CLI) programs built to practice and consolidate core Java fundamentals.

1. To-Do App — A simple task manager that lets you:
 Add and remove tasks
 Sort tasks by different criteria (name, priority, date, etc.)
 Save and load tasks from file for persistence
